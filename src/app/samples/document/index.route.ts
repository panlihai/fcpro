import { Routes } from '@angular/router';
import { DocumentComponent } from './document.component';
export const documentRouters: Routes = [
    {
        path:'system/fcdocumentList',//getstart
        component: DocumentComponent
    }
];
